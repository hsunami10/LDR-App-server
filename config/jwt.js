// Holds secret for JWT hashing
// NOTE: In production, store secret into environment variable instead of this file
// TODO: Change secret to something else later
module.exports = {
  secret: 'hsunami'
};
